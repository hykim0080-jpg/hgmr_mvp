# 한글마루 화면·구역 명칭 가이드

> 소통 시 아래 명칭을 사용합니다. 코드의 DOM id와 1:1로 대응.
> 최종 갱신: 2026-07-04

---

## 1. 최상위 화면 (Screen)

| 공식 명칭 | 설명 | DOM id |
|---|---|---|
| **웰컴 화면** | 첫 진입 화면. 민트 배경 + 하랑이 + 로그인 버튼 3종 | `#login-screen` |
| **이메일 로그인 화면** | 이메일/비밀번호 입력 화면 | `#email-login-screen` |
| **앱 화면** | 로그인 후의 전체 컨테이너 (헤더 + 아래 화면들) | `#app-screen` |
| ├ **홈 화면** | 프로필·통계·학습 시작 | `#home-box` |
| ├ **퀴즈 화면** | 문제 풀이 | `#quiz-box` |
| └ **결과 화면** | 세션 완료 (축하 하랑이) | `#result-box` |

## 2. 공통 요소

| 공식 명칭 | 설명 | DOM id |
|---|---|---|
| **앱 헤더** | 앱 화면 최상단 바 (로고 + 프로필 칩 + 로그아웃) | `#app-screen > header` |
| **프로필 칩** | 헤더의 [미니 하랑이 + 닉네임 + ✏️] — 터치 시 프로필 설정 모달 | `#edit-profile-trigger` |
| **토스트** | 상단에 잠깐 떴다 사라지는 알림 배너 | `#app-toast` |
| **하랑이 모션 오버레이** | 정답/오답 시 화면에 팝업되는 하랑이 애니메이션 | `#harang-motion-box` |

## 3. 웰컴 화면 내부

| 공식 명칭 | 설명 | DOM id |
|---|---|---|
| **말풍선** | "안녕! 오늘도 같이 배워볼까?" | `.harang-bubble` |
| **웰컴 하랑이** | 216px 깜빡이는 하랑이 | (login-screen 내 svg) |
| **워드마크** | Jua체 "한글마루" | `.hm-wordmark` |
| **시작 버튼 스택** | 이메일로 시작하기 / Google로 계속하기 / 로그인 없이 둘러보기 | `#email-start-btn` `#google-login-btn` `#anon-login-btn` |

## 4. 홈 화면 내부

| 공식 명칭 | 설명 | DOM id |
|---|---|---|
| **프로필 아바타** | 원형 민트 배경 + 하랑이 + 레벨 배지 | `.avatar-glow` `#home-harang` `#home-level-badge` |
| **주제 칩** | 아바타 아래 "🎯 전체" — 터치 시 주제 선택 모달 | `#home-topic-chip` |
| **인사 영역** | 칭호 + 닉네임 + 인사말 | `#home-level-title` `#home-nickname` |
| **학습 분석 요약 피드** | 통계 카드 4개 + 레벨 진척도를 담은 회색 박스 | (홈 내 카드 그리드) |
| ├ **연속 학습 카드** | 터치 → 출석 달력 모달 | `#home-streak-card` |
| ├ **학습 어휘 카드** | 터치 → 낱말카드 모달 | `#home-words-card` |
| ├ **평균 정답률 카드** | 터치 → 상세 통계 모달 | `#home-accuracy-card` |
| ├ **업적 달성 카드** | 터치 → 업적 모달 | `#home-badges-card` |
| └ **레벨 진척도 바** | n / 200 EXP | `#home-exp-bar` |
| **학습 시작 버튼** | "오늘의 학습 시작하기 (30개)" | `#start-study-btn` |
| **관리자 단어 관리 버튼** | 관리자 계정에만 표시 | `#open-dev-panel-btn` |

## 5. 퀴즈 화면 내부

| 공식 명칭 | 설명 | DOM id |
|---|---|---|
| **퀴즈 헤더 행** | 나가기 ✕ + 진행바 + 진행 텍스트 | `#quiz-home-btn` `.progress-bar` `#progress-text` |
| **신고 버튼** | 우상단 🚩 (단어 오류 신고) | `#flag-word-btn` |
| **태그 칩** | 문제 위 주제 표시 (예: 수능) | `#word-tag-display` |
| **문장 영역** | 빈칸 ____ 이 있는 예문 | `#sentence-text` |
| **뜻풀이 영역** | Lv 배지 + 단어 뜻 | `#meaning-text` |
| **오답 통계 영역** | 정답 후 표시되는 유저 통계 박스 | `#inline-stats-container` |
| **입력줄** | 정답 보기 + 입력창 + 제출/다음 | `#input-group` (`#show-answer-btn` `#answer-input` `#submit-btn` `#next-btn`) |
| **자모 키보드** | 하단 커스텀 키보드 (자모 키 + 스페이스 + 제출 키) | `#custom-keyboard` (`#kbd-enter-btn`) |

## 6. 모달 (팝업 창)

| 공식 명칭 | 설명 | DOM id |
|---|---|---|
| **프로필 설정 모달** | 포즈 피커 + 목도리 피커 + 닉네임 + 칭호 + 단어 수 | `#profile-modal` (`#pose-picker` `#scarf-picker`) |
| **주제 선택 모달** | 중점 학습 주제 6종 선택 | `#topic-modal` |
| **출석 달력 모달** | 월별 출석 표시 | `#calendar-modal` |
| **낱말카드 모달** | 학습 어휘 4열 그리드 + 상세 뷰 | `#vocab-modal` (`#vocab-list`) |
| **상세 통계 모달** | 지표 8종 목록 | `#stats-modal` |
| **업적 모달** | 배지 5종 | `#achievement-modal` |
| **종료 확인 모달** | 퀴즈 나가기 확인 ("계속 풀기 / 홈으로") | `#exit-confirm-modal` |
| **관리자 검수 패널** | 신고 단어 편집 (관리자 전용) | `#dev-panel-modal` |

## 7. 하랑이 포즈·모션 명칭

| 공식 명칭 | 쓰임 | 프레임 id |
|---|---|---|
| **기본 포즈** (조각 안기) | 웰컴 화면, 프로필 기본 | `#fr-idle-1/2` |
| **물범 박수** | 정답 모션 | `#fr-clap-1/3` |
| **눈물 포즈** | 오답 모션 | `#fr-wrong-1/2` |
| **파티 포즈** | 결과 화면, 레벨업 | `#fr-lv-1/2/3` |
| **지혜 조각** | ㅎ 크리스털 (로고·소품) | `#shard` |
| **목도리 색** | 전역 CSS 변수로 모든 하랑이에 적용 | `--scarf-color` |
