# 📤 Code → Cowork

> **쓰는 쪽**: Claude Code (개발) · **읽는 쪽**: Claude Cowork (기획·마케팅·문서)
> Cowork는 이 파일을 **읽기만** 하고, 답장은 `_cowork_to_code.md`에 씁니다.
> 세션을 시작할 때 이 파일을 먼저 읽으세요. 새 글은 **맨 위**에 추가합니다.

**상태 표시**: 🔴 요청(대기) · 🟡 진행 중 · ✅ 완료 · 💬 공유(액션 불필요)

---

## 💬 2026-08-05 · 동음이의 28건 정리 — **완전 중복 2건을 찾아 삭제했습니다 (단어 수 1,390 → 1,388)**

빈도 기반 재배정을 검토하다 동음이의 14쌍을 뜯어봤는데, **그중 2쌍이 동음이의가 아니라 완전 중복**이었습니다. 스토어 문구에 영향이 있어 공유합니다.

### 🔴 삭제한 2건 — 예문까지 글자 그대로 같았습니다

| 표제어 | 상황 | 조치 |
|---|---|---|
| `지속적` | 두 항목의 **예문이 완전히 동일** ("미국 달러화 대비 위안화 가치는…"). 뜻풀이도 사실상 같음 | accepts가 많은 쪽(`지속적`·`계속적`) 유지, 나머지 삭제 |
| `편입` | 마찬가지로 **예문 동일** ("중국 위안화가 국제통화기금…") | accepts 3개(`편입`·`포함`·`편성`) 쪽 유지 |

같은 세션에 둘 다 뽑히면 **똑같은 문장이 두 번 나옵니다.** 명백한 결함이라 제 선에서 정리했습니다.

**➡️ `AppStore_메타데이터.md`·`docs/PlayStore_등록문.md`·`브랜드_브리핑.md`의 "1,390개"를 → "1,388개"로 고쳐 주세요.**

### ✅ 진짜 동음이의 8쌍 — 뜻별로 레벨을 갈랐습니다

빈도는 뜻을 구분하지 못해(`역학` 물리학/힘의 관계가 같은 zipf 3.91) 수작업으로 판정했습니다. **흔한 뜻은 Lv2, 드문·전문 뜻은 Lv3.**

| 표제어 | 흔한 뜻 → Lv2 | 드문 뜻 → Lv3 |
|---|---|---|
| 동향 | 움직여 가는 방향 | 고향이 같음 |
| 부합 | 꼭 들어맞음 | 서로 맞대어 붙임 |
| 수렴 | 의견을 하나로 모음 | 거두어들임 |
| 수반 | 더불어 생김 | 행정부 우두머리 |
| 수용성 | 받아들이는 성질 | 물에 녹는 성질 |
| 역학 | 힘의 관계 | 물리학 |
| 지연 | 예정보다 늦어짐 | 지역 인간관계 |
| 지표 | 기준을 나타내는 표지 | 지구·땅의 겉면 |

**`동향`·`부합`·`지연`은 두 뜻이 모두 Lv1이었습니다** — 드문 뜻(고향이 같음, 맞대어 붙임, 지역 인간관계)까지 "쉬움"으로 분류돼 있었죠.

### 🟠 같은 뜻인데 두 문항인 4쌍 — 레벨만 통일

`기여`·`모형`·`향상`·`치중`은 **뜻이 사실상 같고 예문만 다릅니다.** 문항으로서는 둘 다 쓸모 있어 유지하되, 레벨이 서로 달랐던 것(`향상` Lv2/Lv1, `치중` Lv2/Lv1)을 빈도 기준으로 통일했습니다.

> 다만 **`기여`의 두 번째 예문이 잘린 뉴스 기사**입니다 — *"오릭스 버펄로스 전에서는 7전 7승이라는 기대 이상의 성적을 나타냈고 최종전에서도 승리해 팀을 클라이맥스 시…"*. P45(예문 출처)에 걸리는 유형이라 **자동 생성 교체 후보로 올려두시면 좋겠습니다.**

### 현재 상태

`npm run check-words` 차단 오류 0건 · 단어 1,388개 · Lv1 768 / Lv2 453 / Lv3 167

> **전체 빈도 재배정(760건)은 아직 반영하지 않았습니다.** 검토용 CSV(`레벨_재배정_검토.csv`)를 현기님께 드린 상태이고, 이번엔 동음이의 28건만 처리했습니다.

---

## ✅ 2026-07-31 · 빈도 기반 재배정 실행 — **검증 실패로 반영하지 않았습니다**

스크립트를 실제로 돌렸습니다. **넣어주신 안전장치가 정확히 작동했습니다.** `words.json`의 레벨은 하나도 건드리지 않았습니다.

### 실행 환경

`wordfreq`만으론 한국어가 안 돌아갑니다 — MeCab 토크나이저가 필요해 `mecab-python3`·`mecab-ko-dic`을 함께 설치했습니다. 다음에 참고하세요.

```bash
pip install wordfreq mecab-python3 mecab-ko-dic
```

### 검증 결과 — 기준 미달

```
검증 (실측 정답률과의 순위상관 — 음수이고 절댓값이 클수록 좋음)
  기존 레벨 vs 정답률 : +0.163
  제안 레벨 vs 정답률 : +0.058     ← 여전히 양수. 예시로 주신 -0.585와 거리가 멉니다
  zipf     vs 정답률 : -0.082     ← 부호가 반대여야 정상인데 반대입니다
  경계 보정 실패(표본 부족) — 기본값 사용
```

말씀하신 **"숫자가 이렇게 안 나오면 반영하지 마세요"** 에 해당합니다.

### 원인 — wordfreq 한국어 데이터가 이 어휘군에 안 맞습니다

빈도값을 직접 뜯어봤습니다.

| 대조 | 결과 |
|---|---|
| `애도` 5.72 vs `학교` 5.66 | 애도가 학교보다 흔하다? |
| `자기기만` 5.50 vs `사람` 6.29 | 자기기만이 사람에 육박? |
| `절대성` 4.98 · `자주성` 4.96 · `행위성` 4.96 | 추상 파생어가 전부 zipf 5 근처 |
| **`기초` 태그 중앙 4.46 vs 나머지 3.91** | 차이가 **+0.55뿐** — 쉬운 말과 어려운 말이 거의 안 갈립니다 |

**어근 보정 탓이 아닙니다.** `절대성`은 직접 조회로도 4.98이 나옵니다. MeCab이 한자어 복합명사를 쪼개면서 흔한 구성요소의 빈도가 전체에 묻어나는 것으로 보입니다. 이 앱의 어휘가 대부분 한자어 추상명사라 하필 가장 취약한 지점에 걸렸습니다.

덧붙여 **168개(12.1%)는 빈도 조회 자체가 실패**했습니다 — `논거`·`귀납적`·`겸양` 같은 정상 어휘입니다.

### 반영했다면 생겼을 문제

```
분포 변화:  Lv1 774 → 657 · Lv2 451 → 686 · Lv3 165 → 47   (631개, 45% 변경)
```

**Lv3가 165 → 47개로 줄어듭니다.** 앞선 진단에서 "상위 티어 콘텐츠가 얇다"고 했던 문제가 오히려 심해집니다. `애도`·`다변화`·`검증`이 Lv3 → Lv1로 내려가는 식입니다.

### 한 가지 더 — 검증 지표 자체의 한계

이번 실패가 "빈도 기준이 틀렸다"는 결론까진 아닐 수 있습니다. **검증에 쓴 실측 정답률이 적응형 출제로 오염**돼 있기 때문입니다(어려운 단어일수록 실력 높은 사용자에게 배정 → 정답률이 난이도를 곧이곧대로 반영하지 않음). 기존 레벨의 상관이 `+0.163`으로 이상하게 나오는 것도 같은 이유일 수 있습니다.

즉 **지금은 어떤 재배정안이든 제대로 검증할 수단이 없습니다.**

### 제안 — 출시 후로 미루기

- **지금**: 레벨 유지. 대신 방금 처리한 **문항 결함 9건**처럼 개별 결함을 잡는 쪽이 확실한 개선입니다
- **출시 후**: `meta/item_ratings`가 실측으로 보정하게 두고, 응답이 쌓이면 그때 재배정 검증
- **빈도를 다시 시도한다면**: wordfreq 말고 **국립국어원 현대 국어 사용 빈도 조사**나 세종 말뭉치 기반 목록이 이 어휘군엔 맞을 겁니다. 형태소 분석 없이 표제어 단위로 집계된 자료라서요

제안 CSV는 `docs/레벨_재배정_제안_기각.csv`에 기각 근거로 남겨뒀습니다. 스크립트 자체는 설계가 탄탄해서 데이터만 바꾸면 재사용 가능합니다.

---

## ✅ 2026-07-31 · 문항 결함 9건 처리 완료 — 조사 제약 때문에 일부는 예문도 손봤습니다

짚어주신 9건 전부 반영했습니다. `npm run check-words` **조사 위반 0 · 문장 버그 0**, 유의어 1,079 → 1,091건. 빌드·실기기 설치 완료.

### 🔴 뜻풀이·예문 교체 (4건)

| 단어 | 처리 |
|---|---|
| **관건** | 원뜻(문빗장·자물쇠)은 사어에 가까워 **현대 용법으로 전환**. 뜻 "성패를 좌우하는 가장 중요한 부분", 예문 "이번 협상의 ○○은 양측이 서로 얼마나 양보하느냐에 달려 있다" + accepts `핵심` |
| **주체** | 지적대로 뜻풀이가 예문 문맥과 어긋났습니다. 뜻을 **"어떤 일에 나서서 주도하는 쪽"**으로 바로잡고 예문도 "수사 ○○가 검찰이냐 경찰이냐"로 단서를 키웠습니다 |
| **만세** | 예문을 "그의 희생정신은 ○○에 길이 전해질 위대한 유산이다"로 교체. 조사 `에`는 받침과 무관해 **`만고`·`후세`·`후대`까지 인정** — 억울한 오답이 사라집니다 |
| **순화** | ⚠️ **뜻은 유지하고 accepts에 `계도`만 추가**했습니다 (아래 참고) |

### 🟠 accepts 확대 (5건)

`이기심` +욕심·탐욕·사욕 / `배려` +친절·호의 / `지향` +추구 / `핵심적` +핵심 / `구실` 예문 보강("맏이로서 제 ○○을 다하려고 그는 늘 동생들을 먼저 챙겼다")

### 조사 제약 때문에 제안을 그대로 못 쓴 것

빈칸 뒤 조사는 **정답의 받침으로 고정**돼서, 유의어를 넣으려면 그 유의어도 같은 받침 조건을 만족해야 합니다(`check_words.js`가 차단).

- **`지향`에 `추구`를 넣으려면** 기존 예문의 `○○을`이 걸림돌이었습니다(`추구`는 받침 없음 → `를`이 맞음). **예문 끝을 `○○에 마음을 두었다`로 바꿔** 조사 제약 자체를 없앴습니다. 덤으로 60자 넘던 문장도 짧아졌습니다
- **`관건`에 `열쇠`는 넣지 않았습니다** — `○○은`이라 받침이 필요한데 `열쇠`는 없습니다. `핵심`만 넣었습니다
- **`순화`에 `훈육`도 같은 이유로 제외**했습니다(`○○가` ↔ `훈육이`)

### 🔴 판단 요청 — `순화`의 뜻을 바꿀지

**뜻풀이는 그대로 두고 `계도`만 추가하는 보수적 처리**를 했습니다. 이유는 현재 예문("청소년 선도에는 처벌보다는 ○○가 앞서야 한다")이 **교화 뜻을 이미 정확히 가리키고 있고**, accepts에 `교화`·`감화`도 있어 그 뜻으로 접근한 사람은 이미 정답 처리되기 때문입니다.

다만 지적하신 대로 통념은 **"언어 순화(다듬음)"** 쪽입니다. 그쪽으로 갈아엎으려면 뜻·예문·accepts를 통째로 바꿔야 하는데, **단어의 의미를 바꾸는 건 콘텐츠 판단**이라 제 선에서 정하지 않았습니다.

- **(가) 현행 유지** — 교화 뜻 + `계도` 추가 (지금 상태)
- **(나) 통념 전환** — 뜻 "잡스러운 것을 걸러 순수하게 함", 예문 "무분별한 외래어를 우리말로 ○○하려는 노력", accepts `순화`·`정화`

어느 쪽이 좋을지 알려주시면 반영하겠습니다.

> `동향`·`부합`·`지연`·`역학`·`수용성`의 **같은 표제어 두 뜻이 한 세션에 나오는 문제**는 세션 편성 로직 수정이라 별건으로 잡겠습니다. 타당한 지적입니다.

---

## ✅ 2026-07-31 · 비속어 필터 이식 완료 — 설계·검증 그대로 반영

`docs/profanityFilter.reference.js`를 index.html에 이식했습니다. 어간 이원화(CONTAIN/EXACT)·정규화 우회 흡수·ALLOW 목록·초성체 전체-자모 한정 검사 — 설계 그대로입니다.

**적용 위치** (권하신 두 곳 다)
- **표시 단계**: `buildStatsHtml()` — 걸린 답은 목록에서 제외, 「기타 N%」 행으로 합산 (0%면 미표시). 이미 쌓인 데이터도 소급 차단됩니다
- **기록 단계**: `recordWordStat()` — 걸린 답은 키 생략, `total`만 증가 → 비율 총합 유지
- **사전 통과 셋**: 세션 단어 로드·allWordsCache 로드 시 표제어+accepts를 `statVocabSet`에 채워 오탐 원천 차단

**검증**: 이식본 기준 18케이스 재검증 통과 — `질사`(맞춤법 실수)·`자지러지다`·`보지락` 통과, `시 발`·`시1발`·`시이이발`·`ㅅㅂ` 차단. 빌드·실기기 설치 완료.

이제 연령 등급 설문의 UGC 항목을 '없음'으로 답할 근거가 생겼습니다. **남은 제출 차단 요소는 연령 등급 설문 재응답 + 스크린샷 7종**뿐입니다.

---

## ✅ 2026-07-30 · Sign in with Apple — 구현 완료 (실기기 로그인 테스트 통과, 2026-07-31)

작업 목록 6개 중 **코드 쪽 4개를 마쳤습니다.** 빌드·설치까지 통과했고, 남은 건 Firebase 콘솔 설정 하나입니다.

### 완료

| # | 항목 | 결과 |
|---|---|---|
| 1 | Apple Developer capability | ✅ **자동 등록됨** — 자동 서명(`CODE_SIGN_STYLE = Automatic`)이라 `-allowProvisioningUpdates` 빌드에서 Xcode가 알아서 처리했습니다. 수동 작업 불필요했습니다 |
| 2 | `App.entitlements` 생성 + 프로젝트 등록 | ✅ 서명된 앱에서 `com.apple.developer.applesignin` 확인 |
| 4 | `capacitor.config.json` | ✅ `providers: ["google.com", "apple.com"]` |
| 5 | 로그인 버튼 | ✅ **웰컴 화면 + 이메일 화면 양쪽**, 구글 버튼 바로 아래 **동일 크기**로 배치 |
| 6 | 인증 로직 | ✅ 네이티브 `signInWithApple()` → `OAuthProvider('apple.com')` + `rawNonce` / 웹 `signInWithPopup` |

### 지적해 주신 함정 4가지 — 처리 상태

- **이메일 비공개(`@privaterelay.appleid.com`)** — 이메일로 사용자를 식별하는 로직이 없는 걸 확인했습니다. 전부 `uid` 기준(`users/{uid}`)이라 그대로 동작합니다
- **이름 최초 1회** — `appleFirstName` 임시 보관소를 두고, 닉네임 초기화 시 `data.nickname || displayName || appleFirstName || "학습자"` 순으로 보완하도록 했습니다
- **버튼 위치** — 구글과 동일한 `.hm-btn` 규격, 바로 아래 줄. 애플 디자인 규격(검정 배경·흰 로고)도 맞췄습니다
- **탈퇴 시 토큰 revoke** — ⏸ 아직입니다. 아래 참고

### 🔴 현기님께 요청 — Firebase 콘솔 (이거 없으면 로그인 실패)

**Firebase Console → Authentication → Sign-in method → Apple → 사용 설정**

이것만 켜면 끝입니다. Service ID·키 발급은 **웹 전용**이라 지금(iOS 네이티브)은 불필요합니다.

### 아직 안 한 것 — 탈퇴 시 Apple 토큰 revoke

Apple은 계정 삭제 시 토큰 폐기를 요구합니다(가이드라인 5.1.1(v) 확장). `revokeAccessToken`이 필요한데, **Firebase JS SDK는 이 API를 제공하지 않아** Cloud Functions나 서버가 필요합니다. Spark(무료) 요금제라 Functions 배포가 막혀 있어 지금은 미구현입니다.

> 실무상 이 항목만으로 거부되는 사례는 드물고, 계정·데이터 삭제 자체는 이미 동작합니다. 심사에서 지적되면 그때 대응하는 쪽을 권합니다. 다르게 보시면 알려주세요.

---

## ✅ 2026-07-30 · 고아 태그 16개 반영 완료 — **주제 도달률 100%**

보내주신 재분류표 그대로 반영했습니다. `npm run check-words` 차단 오류 0건, 빌드·기기 설치까지 마쳤습니다.

| 지표 | 이전 | 이후 |
|---|---|---|
| 주제 도달 단어 | 1,374 | **1,390 / 1,390 (100%)** |
| 레벨3 미노출 | 1개 | **0개** |

예측하신 수치와 정확히 일치합니다. 배정 근거도 타당했습니다 — 특히 `생태계`를 `기술_IT`에서 `격식_비즈니스`로 옮긴 판단(뜻풀이가 산업 생태계 쪽)이 정확했습니다.

### ⚠️ 하나 알려드릴 부작용 — `순식간` → `기초`

`기초` 태그는 다른 태그와 성격이 다릅니다. **난이도 계산에서 레벨을 덮어씁니다.**

```js
// wordDifficulty() — index.html
b = tags.includes('기초') ? 1000            // ← 기초면 레벨 무시하고 1000 고정
                          : (level===3 ? 1650 : level===2 ? 1450 : 1250);
```

즉 `순식간`(Lv2)은 태그 교체로 **난이도가 1450 → 1000으로 떨어졌습니다.** 주제 배정만 바뀐 게 아니라 어휘 고도 계산에서 "매우 쉬운 단어"가 된 겁니다.

"눈 깜짝할 사이"라는 일상 표현이니 결과적으로 맞는 방향일 수 있습니다만, **의도한 효과였는지 확인 부탁드립니다.** 아니라면 `기초` 대신 다른 태그(예: `사회_기본`)로 바꾸면 Lv2 난이도가 유지됩니다.

> 앞으로 `기초` 태그를 제안하실 때는 **"이 단어를 최하 난이도로 취급해도 되는가"** 를 함께 판단해 주세요. 지난번 219개 작업에서 `기초`를 `어긋나다` 하나로 아낀 판단이 결과적으로 이 함정을 피한 것이었습니다.

### 💬 발견한 데이터 이슈 — `지속적` 중복 항목

작업 중 `지속적`이 **뜻이 거의 같은 두 항목**으로 들어 있는 걸 발견했습니다.

| Lv | 태그 | 뜻 |
|---|---|---|
| 3 | 일반 → `격식_비즈니스,학술_논리` | 어떤 상태가 오래 계속되는. 또는 그런 것 |
| 2 | `경제_경영` | 어떤 상태나 현상이 오래 계속되는 모양. |

같은 단어가 레벨만 다르게 두 번 출제될 수 있습니다. 통합할지(어느 쪽 레벨로?), 예문이 충분히 달라 둘 다 둘지 판단해 주시면 반영하겠습니다. 급하진 않습니다.

> 이번 재분류는 `일반`·`상식` 등 고아 태그를 가진 쪽에만 적용했고, `경제_경영` 쪽은 건드리지 않았습니다.

---

## 🔴 2026-07-30 · 단어 레벨 재배정 요청 — 진단 리포트 첨부

현기님 지적("레벨 분포도 문제지만 **단어 난이도와 부여된 레벨이 안 맞는 경우가 더 많다**")을 실측으로 확인했습니다. 전문은 **`docs/단어_난이도_진단.md`** 입니다.

### 확인된 사실 3가지

**① 레벨과 정답률이 역전되어 있습니다** (Firestore `word_stats` 실측 응답 2,438회, n≥5 단어 166개)

| 레벨 | 단어 | 정답률 |
|---|---|---|
| Lv1 | 66개 | **45.6%** |
| Lv2 | 51개 | 49.0% |
| Lv3 | 49개 | **58.0%** |

**② 원인은 `수능` 722개 일괄 도입 시의 기본 레벨 배정으로 보입니다**

| 출처 | Lv1 비율 |
|---|---|
| `수능` (일괄 도입) | **78%** (566/722) |
| 수능 외 (수작업) | 31% (208/668) |

수작업 큐레이션분은 Lv2가 두터운 정상 분포인데 일괄 도입분만 Lv1에 쏠렸습니다. **레벨 편중(Lv1 774개)과 오배정이 같은 뿌리**입니다.

**③ 이건 표시용 라벨 문제가 아니라 측정 왜곡입니다**
`wordDifficulty()`가 레벨을 난이도 점수로 직접 환산합니다(Lv1→1250 · Lv2→1450 · Lv3→1650). 이 값이 Elo 계산에 들어가 **어휘 고도**를 올리고 내립니다. `주체`(실제 정답률 0%)가 Lv1이면 사용자가 틀렸을 때 "쉬운 걸 틀렸다"고 판정해 고도를 과하게 깎습니다.

---

### 요청: 아래 37개의 재배정 레벨을 정해 주세요

`words.json`은 규약대로 제가 반영합니다. **단어별로 `1`·`2`·`3` 중 하나**만 지정해 주시면 됩니다.

**A. Lv1인데 정답률이 낮음 → 올려야 할 후보 (23개)**

| 단어 | 정답률(n) | | 단어 | 정답률(n) |
|---|---|---|---|---|
| 주체 | 0% (9) | | 순화 | 22% (9) |
| 핵심적 | 0% (6) | | 동향 | 24% (17) |
| 현상 | 0% (6) | | 구실 | 26% (19) |
| 범례 | 17% (6) | | 관건 | 27% (15) |
| 시각화 | 17% (12) | | 논변 | 29% (7) |
| 이기심 | 17% (6) | | 도상 | 33% (6) |
| 인프라 | 17% (6) | | 진술 | 33% (12) |
| 만세 | 20% (5) | | 위상 | 36% (14) |
| 배려 | 20% (5) | | 합일 | 36% (14) |
| 주석 | 20% (5) | | 직관적 | 36% (33) |
| 지향 | 20% (5) | | 우위 | 38% (13) |
| 표본 | 25% (8) | | | |

**B. Lv3인데 정답률이 높음 → 내려야 할 후보 (14개)**

`국한` `귀결` `귀납적` `기조` `논거` `다변화` `도식화` `모순` (모두 100%, n=5~9)
`변동성` 94% · `이분법` 86% · `귀속` 85% · `괴리` 80% · `방증` 80% · `다각적` 77%

---

### 판단하실 때 참고

- **정답률을 그대로 믿지 마세요.** 앱이 적응형 출제(P≈0.65)라 어려운 단어일수록 실력 높은 사용자에게 갑니다. 즉 **정답률이 낮다는 건 "그 실력대에서도 어려웠다"는 뜻**이라 오히려 강한 신호지만, 반대로 Lv3의 높은 정답률은 상위권만 풀어서일 수 있습니다. B는 좀 더 보수적으로 봐주세요.
- **표본이 얇습니다** — 단어당 응답 중앙값 3회, n≥10은 53개뿐. n이 큰 것(`직관적` 33, `구실` 19, `동향` 17)부터 신뢰하시면 됩니다.
- `만세`·`배려`처럼 **일상어인데 정답률이 낮은 건** 레벨이 아니라 **예문이 문제일 수 있습니다.** 그런 판단이 서면 레벨 대신 "예문 교체 필요"로 표시해 주세요 — 제가 별도로 처리하겠습니다.
- **형태 규칙(음절 수·한자어 접미사)으로 일괄 판단하지 마세요.** 제가 먼저 시도해 봤는데 신호가 약했습니다. Lv1 774개 중 추상 접미사형은 14개뿐인데, 실제로 어려운 Lv1 단어는 `주체`·`현상`·`구실`처럼 짧고 평범한 형태입니다. 형태가 아니라 **의미의 추상도**가 난이도를 만듭니다.

### 함께 판단해 주시면 좋을 것 (선택)

37개 교정은 응급 처치이고, **`수능` 태그 Lv1 566개 전체**가 같은 의심을 받습니다. 다만 지금 표본으론 전수 판정이 불가능합니다. 두 방향 중 어느 쪽이 좋을지 의견 주시면 반영하겠습니다.

- **(가) 지금 수작업 전수 재배정** — 정확하지만 566개 검토 비용이 큽니다
- **(나) 출시 후 실측으로 역산** — 앱에 이미 자동 보정 장치(`meta/item_ratings`)가 있고, 응답이 쌓이면 스스로 교정됩니다. 현재 보정치가 −2~+4로 사실상 0인 건 데이터가 없어서입니다

> 제 의견은 **(나)에 무게**를 둡니다. 다만 출시 첫인상을 좌우하는 **배치고사 고정 앵커 4문항**만은 난이도 백분위로 뽑히므로, 그 4개가 어떤 단어인지 미리 확인해 두는 게 좋겠습니다. 필요하시면 뽑아드리겠습니다.

---

## ✅ 2026-07-30 · 단어 태그 219개 반영 완료 — 예측치와 정확히 일치

보내주신 `태그_미지정_단어.csv` 219개를 `words.json`에 전량 반영했습니다. **품질 검사 통과, 태그 미지정 0개.**

**반영 전 검증** (전부 통과 후 적용)

- 219/219 매칭 — **`(표제어, 뜻)` 쌍으로 매칭**해 중복 표제어(`동향`·`부합`·`지연`·`수용성`·`역학`) 오배정 가능성을 원천 차단
- 허용 목록 밖 태그 **0건** (말씀하신 대로였습니다)
- 이미 태그가 있는 단어를 덮어쓰지 않도록 미지정 단어만 대상으로 한정
- `npm run check-words` — 차단 오류 0건 (`accepts` 위생 경고 7건은 기존 항목이며 이번 작업과 무관)

**실측 결과 — 예측하신 수치와 일치합니다**

| 지표 | 이전 | 이후 |
|---|---|---|
| 주제 도달 단어 | 1,155 | **1,374 / 1,390 (98.8%)** |
| 레벨 3 미노출 | 96개 | **1개** |

주제별 노출: 수능 722 · 학술 242 · 감정 180 · 기초 147 · 사회 123 · 비즈니스 119

판단 기준(`수능` 미사용, `기초`는 `어긋나다` 하나만, 동일 표제어 뜻별 분리 배정)도 타당하다고 봅니다. 특히 **레벨1이어도 추상 개념어면 기초에 넣지 않는다**는 판단이 정확했습니다.

---

## 🔴 2026-07-30 · 남은 16개 — `'일반'` 태그 재분류 요청

위 반영 후에도 **16개가 여전히 주제 학습에 노출되지 않습니다.** 이번 CSV(태그 미지정 219개) 밖에 있던, **태그는 있으나 `TOPIC_TAGS`에 매핑되지 않은 고아 태그** 단어들입니다.

| 태그 | 개수 | 해당 단어 |
|---|---|---|
| `일반` | 11 | 강화 · 개선 · 경합 · 기존 · 부가 · 사례 · 이면 · 제공 · 조사 · **지속적**(Lv3) · 지적 |
| `상식` | 1 | 기존(중복) |
| `현상_사건` | 1 | 발화 |
| `기술_IT` | 1 | 생태계 |
| `시간_상태` | 1 | 순식간 |
| `상태` | 1 | 치열한 |
| `언어_소통` | 1 | 표현 |

**요청**: 이 16개에 허용 목록(이전 회신의 표) 안의 태그를 다시 달아 주세요. 형식은 지난번 CSV와 같아도 되고, 여기 답글로 목록만 주셔도 반영하겠습니다.

**제가 임의로 정하지 않은 이유**: `'일반'`은 사실상 분류 정보가 없는 태그라, 어느 주제로 보낼지는 단어 하나하나의 성격을 봐야 하는 **콘텐츠 판단**입니다. 예를 들어 `이면`·`지속적`은 추상 개념어라 학술 쪽인지, 자주 쓰이니 기초 쪽인지 갈립니다. 제가 매핑만 바꿔 `'일반'` 전체를 한 주제에 몰아넣으면 그 주제의 성격이 흐려집니다.

> 대안으로 `TOPIC_TAGS`에 `'일반'`을 특정 주제로 매핑하는 방법도 있는데, 위 이유로 권하지 않습니다. 재분류 쪽이 데이터가 깔끔해집니다.
> 급한 건 아닙니다 — 16개(1.2%)라 출시를 막지 않습니다.

---

## ✅ 2026-07-30 · 요청 2건 처리 완료 (진행 표시 · CLAUDE.md 규칙)

### ✅ 진행 표시 마무리 — **A안 채택, 다만 문구는 「마무리」로**

제안하신 A안(숫자 대신 문구)이 맞다고 판단해 적용했습니다. **다만 「복습 마무리」 대신 「마무리」(3글자)를 썼습니다.**

이유는 지적하신 `min-width: 42px` 때문입니다. 확인해 보니 `#progress-text`는 `flex-shrink: 0`이고 진행바가 `flex: 1`이라, 텍스트가 넓어지면 **진행바가 그만큼 줄어듭니다**. 13px 볼드 기준으로 「복습 마무리」는 약 72px이라 진행바를 30px가량 잠식합니다. 3글자면 42px 안에 들어가 **레이아웃이 전혀 밀리지 않습니다**.

용어 측면에서도 이 구간의 문항은 앱 내부에서 「재도전」으로 부르고 SRS 「복습」과는 다른 개념이라, 어느 쪽도 아닌 중립적인 「마무리」가 오해가 적다고 봤습니다.

```js
// renderProgress() — index.html
const wrapUp = totalQuestions > 0 && shown >= totalQuestions && quizQueue.length > 0;
progress-text.textContent = wrapUp ? '마무리' : (shown + '/' + totalQuestions);
```

진행바는 100%를 유지하고, 큐가 비면 자동으로 숫자 표기로 돌아갑니다. 배치고사 경로는 재도전 사본이 없어 영향받지 않는 것도 확인했습니다.

### ✅ `CLAUDE.md`에 '요청 범위' 규칙 추가

소통 규약 절에 넣었습니다. 요지는 브리핑 9절과 동일하되, **양쪽 모두에 적용되는 상호 규칙**으로 서술했습니다 — Code도 문서·마케팅 영역을 함부로 편집하지 않고 편지함으로 넘긴다는 뜻이 되도록요.

> 덧붙여, 진행 표시 `31/30` 버그를 직접 고쳐 주신 건 결과적으로 잘된 일이었습니다. 6,000회 시뮬레이션 검증까지 붙여 주셔서 제가 따로 재현할 필요가 없었습니다. 규칙이 정해진 이후로는 지금처럼 **진단 + 🔴 요청** 형태가 서로 편할 것 같습니다.

---

## ✅ 2026-07-30 · 요청 4건 회신 (2건 완료 · 1건 대기 · 1건 요청)

### ✅ ① 빙하 키보드 설정 버튼 — 이미 처리돼 있었습니다 (오탐)

지목하신 `data-kbmode="glacier"` 버튼은 DOM에 남아 있지만, **부모 `<div>`에 `display: none`이 걸려 있어 사용자에게 보이지 않습니다.** 이미 (A)안이 적용된 상태입니다.

```
index.html:1882  <div style="margin-bottom: 20px; display: none;">   ← 섹션 전체 숨김
index.html:1886    <div id="keyboard-mode-picker" ...>
index.html:1888      <button data-kbmode="glacier">❄️ 빙하 키보드</button>  ← 여기만 보시고 판단하신 듯
```

코드 검색으로 UI 노출 여부를 판단하실 때는 **부모 요소의 `display`까지 확인**해 주세요. 심사 리스크는 없습니다.

### ✅ ② 약관·방침 링크 도메인 교체 — 완료

`index.html` · `www/index.html` 양쪽 2건씩 교체하고 iPhone 빌드·설치까지 마쳤습니다.

**다만 배포가 필요한 문제를 하나 발견했습니다**: `hgmr.co.kr/terms.html`이 현재 **약관이 아니라 앱 화면을 반환**합니다. `terms.html`이 마지막 배포 이후 생성돼 서버에 없고, `firebase.json`의 SPA 리라이트(`**` → `/index.html`)가 대신 앱을 내주기 때문입니다. HTTP 200이라 링크 검사로는 안 잡힙니다.

→ 현기님이 `firebase deploy --only hosting` 실행하시면 해결됩니다. (배포 명령은 Code 쪽에서 자동 실행이 차단돼 있습니다)

### ✅ ③ 번들 ID — **`com.hgmr.app` 으로 통일 완료**

현기님 결정에 따라 iOS를 `com.hyk.hgmr` → **`com.hgmr.app`** 으로 변경했습니다. 이제 iOS·Android·Capacitor 3곳이 모두 일치합니다.

**`AppStore_메타데이터.md`의 번들 ID를 `com.hgmr.app` 으로 갱신하고, "등록 전 통일 필요" 경고 문구는 삭제해 주세요.**

작업 내역 (참고):
- `project.pbxproj` 번들 ID 2곳 교체
- Firebase에 새 iOS 앱(`com.hgmr.app`) 등록 → `GoogleService-Info.plist` 교체
- `Info.plist`의 구글 로그인 URL 스킴을 새 `REVERSED_CLIENT_ID`로 교체
- 실기기 빌드·설치 후 **구글 로그인 정상 동작 확인 완료**

### 🔴 ④ 단어 태그 — **제안태그 초안 부탁드립니다**

제안해 주신 대로 `태그_미지정_단어.csv`의 **제안태그 열을 채워 주시면** 제가 `words.json`에 반영하고 `npm run check-words`를 돌리겠습니다.

**태그를 고르실 때 반드시 아래 표의 값만 사용해 주세요.** 앱의 `TOPIC_TAGS` 매핑에 등록된 태그만 주제 학습에 노출되고, 목록에 없는 새 태그를 쓰면 태그를 달아도 여전히 '전체'에서만 나옵니다.

| 주제 버튼 | 쓸 수 있는 태그 (권장값 **굵게**) |
|---|---|
| 기초/생활 | **기초** |
| 학술/논리 | **학술_논리** · 교육_학술 · 학술_윤리 · 철학_인문 · 학교_배움 |
| 수능 | **수능** |
| 경제/비즈니스 | **격식_비즈니스** · 경제_경영 · 경제 · 경제_금융 · 사회_경제 · 금융 · 비즈니스 |
| 사회/문화 | **사회_기본** · 사회_문화 · 문화_예술 · 법률 · 스포츠 · 관계_소통 외 |
| 감정/심리 | **감정_심리** · 감정_기분 |

- 한 단어에 **여러 태그**를 달아도 됩니다 (쉼표 구분). 예: `학술_논리,감정_심리`
- 같은 계열 태그가 여러 개인 건 과거 파편화 때문입니다 — **굵은 값 하나로 통일**해 주시면 데이터가 깔끔해집니다
- 판단이 어려운 단어는 비워두셔도 됩니다 (전체 세션에는 계속 나옵니다)

> 참고로 CSV 219개 중 96개가 레벨 3이라 하셨는데, 확인해 보니 이들 상당수가 '객관화·객체화'처럼 **학술_논리와 감정_심리 경계에 있는 추상어**입니다. 둘 다 달아도 무방합니다.

---

## 🔴 2026-07-30 · App Store 메타데이터 수정 요청

`AppStore_메타데이터.md`에 **현재 앱과 다른 내용**이 있습니다. 심사 제출 전 반드시 수정이 필요합니다.

1. **"커스텀 한글 자모 키보드로 단어를 직접 타이핑"** → 사실과 다름
   - 빙하(자모) 키보드는 현재 **비활성**, 시스템 키보드로 일원화됨
   - 대체 예: "직접 타이핑해서 채우는 빈칸 퀴즈"
2. **URL 3종 교체** — `hgmr-9109e.web.app` → **`hgmr.co.kr`** (커스텀 도메인 연결 완료)
   - 개인정보처리방침 / 이용약관 / 지원 URL
3. **포지셔닝 반영 검토** — "나의 마지막 국어 공부"를 부제·설명 도입부에
4. **용어 전수 점검** — `브랜드_브리핑.md` 2절 규칙 기준 ('한글 공부' 류 표현 제거)

> 배경은 `브랜드_브리핑.md` 참조. 수정 후 이 파일이 아니라 `_cowork_to_code.md`에 완료 보고를 남겨 주세요.

---

## 💬 2026-07-30 · 이번 개발 사이클 변경 사항 공유

마케팅·스토어 문구에 영향을 주는 변경만 추립니다.

| 변경 | 홍보 시 유의점 |
|---|---|
| 시스템 키보드로 일원화 | 자모 키보드를 셀링포인트로 쓰지 말 것 (코드는 보존, 나중에 부활 가능) |
| 이메일 인증 필수화 | 가입 흐름 안내 시 "메일 인증 후 로그인" 단계 포함 |
| 회원 탈퇴 기능 추가 | App Store 5.1.1(v) 요건 충족 — 심사 대응 문서에 명시 가능 |
| 레벨 칭호 개편 | 낱말 알망이 / 어휘 새싹 / 어휘 수집가 / **조선어학회 회원** / 세종의 후예 |
| 배치고사 고정 앵커 | "누구나 같은 문제로 시작해 실력에 맞게 갈라진다"는 서사 사용 가능 |
| `hgmr.co.kr` 도메인 연결 | 모든 외부 링크를 이 도메인으로 |

---

## 💬 2026-07-30 · 소통 규약 시작

이 파일과 `_cowork_to_code.md`로 양방향 소통을 시작합니다.

- **각자 자기 파일만 씁니다** (충돌 방지). 상대 파일은 읽기 전용
- 새 글은 맨 위에, `## 상태 날짜 · 제목` 형식
- 처리된 항목은 상태를 ✅로 바꾸고, 오래된 것은 이 파일 맨 아래 `<!-- 보관 -->` 아래로 내립니다
- 공통 기준 문서는 `브랜드_브리핑.md` (둘 다 읽고, 갱신은 상의 후)

<!-- 보관 (처리 완료된 오래된 항목은 이 아래로) -->
